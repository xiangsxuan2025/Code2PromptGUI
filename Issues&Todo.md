# 发现的问题 与 ToDo
- 大工程, 内存会飙升, 导致卡顿甚至假死. 可能原因:
   - 读取文件时, 大文件没有限制大小, 导致内存占用过高.
   - 处理文件内容时, 没有流式处理, 一次性加载所有内容到内存.
- 输出文件名目前写死,可以用目录名
- rust程序, 只调用了基本的输出API, 其他参数传进去不确定是否有效
- UI还不完善,Advanced也么有调整布局
- 会把两个文件写到一起?有时候会把两个工程的内容写到一起?
